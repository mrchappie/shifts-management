import { CustomUsersFilterPipe } from './custom-users-filter.pipe';

describe('CustomUsersFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomUsersFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
